package learnj.topics.p2_s09_lambda_stream;

import learnj.TopicRegistry;

/**
 * LearnJava placeholder
 * <p>
 * Doc      : 第2部分-阶段9-Lambda-方法引用-Optional-Stream-Collector.md
 * Stage    : p2_s09_lambda_stream
 * Step     : s08
 * Item     : lambda_checked_exceptions
 * Topic id : part2/stage09/s08/lambda_checked_exceptions
 * Heading  : lambda 与 checked 异常
 * <p>
 * TODO: read the JavaStudy section, write a minimal runnable demo,
 *       then replace this empty body with real practice code.
 */
public final class s08_lambda_checked_exceptions {
    static {
        TopicRegistry.register("part2/stage09/s08/lambda_checked_exceptions", args -> {
            // practice: lambda 与 checked 异常
            return 0;
        });
    }

    private s08_lambda_checked_exceptions() {
    }
}
