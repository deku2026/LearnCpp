package learnj.topics;

import learnj.TopicRegistry;

/**
 * LearnJava placeholder
 * <p>
 * Doc      : 第2部分-阶段3-控制流与模式匹配.md
 * Stage    : p2_s03_control
 * Step     : 步骤 3
 * Item     : break_continue_return
 * Topic id : part2/stage03/s03/break_continue_return
 * Heading  : `break` / `continue` / `return` / 标签
 * <p>
 * TODO: read the JavaStudy section, write a minimal runnable demo,
 *       then replace this empty body with real practice code.
 */
public final class P2S03ControlS03BreakContinueReturn {
    static {
        TopicRegistry.register("part2/stage03/s03/break_continue_return", args -> {
            // practice: `break` / `continue` / `return` / 标签
            return 0;
        });
    }

    private P2S03ControlS03BreakContinueReturn() {
    }
}
