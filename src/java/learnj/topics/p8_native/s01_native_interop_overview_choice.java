package learnj.topics.p8_native;

import learnj.TopicRegistry;

/**
 * LearnJava placeholder
 * <p>
 * Doc      : 第8部分-本地互操作-JNI-FFM-Vector-API.md
 * Stage    : p8_native
 * Step     : s01
 * Item     : native_interop_overview_choice
 * Topic id : part8/native/s01/native_interop_overview_choice
 * Heading  : 本地互操作全景与选型
 * <p>
 * TODO: read the JavaStudy section, write a minimal runnable demo,
 *       then replace this empty body with real practice code.
 */
public final class s01_native_interop_overview_choice {
    static {
        TopicRegistry.register("part8/native/s01/native_interop_overview_choice", args -> {
            // practice: 本地互操作全景与选型
            return 0;
        });
    }

    private s01_native_interop_overview_choice() {
    }
}
