@file:JvmName("P2S05NullTypesS04IsAsSmartCastKtFacade")

package learn.topics

import learn.registerTopic

// LearnKotlin placeholder
// Doc      : 第2部分-阶段5-可空性与类型系统.md
// Stage    : p2_s05_null_types
// Step     : s04
// Item     : is_as_smart_cast
// Topic id : part2/stage05/s04/is_as_smart_cast
// Heading  : `is` / `!is` / `as` / `as?` 与 Smart cast
//
// TODO: read the KotlinStudy section, write a minimal runnable demo,
//       then replace this empty body with real practice code.

@Suppress("unused")
private val REGISTER = registerTopic("part2/stage05/s04/is_as_smart_cast") { _ ->
    // practice: `is` / `!is` / `as` / `as?` 与 Smart cast
    0
}
