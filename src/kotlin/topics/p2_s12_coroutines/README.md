# p2_s12_coroutines

Doc: `第2部分-阶段12-协程与结构化并发.md`

Each `sNN_*.kt` is an independent practice unit (placeholder).
Register via `registerTopic("...") { ... }` — same role as C++ `learn::topic<...>`.

```text
./gradlew run --args="--list"
./gradlew run --args="<topic_id>"
```
