@file:JvmName("P2S12CoroutinesS03CoroutinescopeSupervisorscopeScopeBuildKtFacade")

package learn.topics

import learn.registerTopic

// LearnKotlin placeholder
// Doc      : 第2部分-阶段12-协程与结构化并发.md
// Stage    : p2_s12_coroutines
// Step     : s03
// Item     : coroutinescope_supervisorscope_scope_build
// Topic id : part2/stage12/s03/coroutinescope_supervisorscope_scope_build
// Heading  : Scope 构建器：`coroutineScope` / `supervisorScope` / 所有权
//
// TODO: read the KotlinStudy section, write a minimal runnable demo,
//       then replace this empty body with real practice code.

@Suppress("unused")
private val REGISTER = registerTopic("part2/stage12/s03/coroutinescope_supervisorscope_scope_build") { _ ->
    // practice: Scope 构建器：`coroutineScope` / `supervisorScope` / 所有权
    0
}
