# LearnKotlin sources (`src/kotlin`)

Source dir is set in root `build.gradle.kts` (`sourceSets.main.kotlin.srcDirs`).
Parallel to `src/cpp/`. Open repo root in IDEA; run config `.run/LearnKotlin_Main.run.xml`.
