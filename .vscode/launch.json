{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach C++ (learn_ffm → java)",
      "type": "cppvsdbg",
      "request": "attach",
      "processId": "${command:pickProcess}",
      "symbolSearchPath": "${workspaceFolder}/build/windows-debug/bin",
      "logging": {
        "moduleLoad": true
      }
    }
  ]
}
